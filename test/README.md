# On macOS
gcc -o test/testlib test/test.c -L target/release -lfile_metadata

# On Linux
gcc -o test/testlib test/test.c -L target/release -lfile_metadata

# On Windows
gcc -o test/testlib test/test.c -L target/release -lfile_metadata